{
  "name": "photopea-fullwidth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "capture:store": "npm run build && node scripts/capture-store-assets.mjs",
    "check:photopea": "npm run build && node scripts/check-photopea.mjs",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "package": "npm run build && node scripts/package.mjs",
    "verify": "npm run fmt:check && npm run lint && npm run build && node scripts/verify.mjs"
  },
  "devDependencies": {
    "oxfmt": "0.66.0",
    "oxlint": "1.81.0",
    "playwright": "1.62.1"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
